<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArtAttack.FinalisePurchase"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtAttack"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#cccaca"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="Black"/>
        </Style>
    </Page.Resources>

    <Grid  Padding="20,20,20,0" RowSpacing="5" ColumnSpacing="60" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Thank you for your purchase!" Grid.Row="0" Grid.Column="0" FontSize="20"/>
        
        <ListView x:Name="productList" Background="#cccaca" Grid.Row="1" Grid.Column="1" ></ListView>
        
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top" Grid.Row="2" Grid.Column="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Subtotal: "/>
                <TextBlock x:Name="subtotal"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Delivery fee: "/>
                <TextBlock x:Name="delivery"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Total: "/>
                <TextBlock x:Name="total"/>
            </StackPanel>

        </StackPanel>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top" Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Full Name: "/>
                <TextBlock x:Name="fullName"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Phone Number: "/>
                <TextBlock x:Name="phoneNumber"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Email: "/>
                <TextBlock x:Name="email"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Payment Method: "/>
                <TextBlock x:Name="payMethod"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Order Status: "/>
                <TextBlock x:Name="orderStatus"/>
            </StackPanel>
        </StackPanel>
        <Button x:Name="backButton"  Grid.Row="3" VerticalAlignment="Bottom" Foreground="Black" Background="#cccaca">Back to shop</Button>

    </Grid>
</Page>
